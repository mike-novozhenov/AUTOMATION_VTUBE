# Automation VTube Project

Проект по автоматизации тестирования (Web UI) на базе **Python + Selenium + PyTest**.
Данный репозиторий демонстрирует применение паттерна **Page Object Model (POM)** и архитектурные подходы к построению масштабируемого тестового фреймворка.

## 🛠 Tech stack
* **Language**: Python 3.x
* **Test Runner**: PyTest
* **Web Automation**: Selenium WebDriver
* **Design Pattern**: Page Object Model (POM)
* **Reporting**: Allure Reports (optional)
* **Config Management**: Python-dotenv / Settings.py

## 📂 Project structure
* `pages/` — Page Objects: описание логики страниц и локаторов.
* `tests/` — Тестовые сценарии (Test Suites).
* `config/` — Глобальные настройки (URL, таймауты, учетные данные).
* `conftest.py` — Конфигурация PyTest и фикстуры (управление жизненным циклом WebDriver).
* `utils/` — Вспомогательные инструменты (хелперы для работы с данными, логами).

## 🐳 Docker Support

Проект подготовлен для запуска в изолированном контейнере. Это гарантирует идентичность окружения при запуске на CI/CD или локально.

## 🚀 Quick start

### 1. Preparing the environment
Клонируйте репозиторий и создайте виртуальное окружение:
```bash
git clone <url_вашего_репозитория>
cd AUTOMATION_VTUBE
python -m venv venv
source venv/bin/activate  # for Linux/macOS
# or
venv\Scripts\activate  # for Windows